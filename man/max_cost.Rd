% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils_normalize_data.R
\name{max_cost}
\alias{max_cost}
\title{Get Maximum Cost Value}
\usage{
max_cost(x, col_starts_with = "Cost")
}
\arguments{
\item{x}{A \code{tibble} containing raw planning unit data (e.g., \code{curr_pu_data}).}

\item{col_starts_with}{A \code{character} string indicating the prefix for cost
columns. Default is \code{"Cost"}.}
}
\value{
A \code{numeric} value representing the maximum cost.
}
\description{
Extract the highest cost value among all cost-related columns in a tibble.
This is useful to determine whether normalization is required.
}
\details{
The function scans all columns that start with \code{col_starts_with}, selects
the highest cost value, and returns it as a numeric value.
}
\examples{
library(dplyr)

# Example tibble
df <- tibble(
  ID = 1:3,
  CostA = c(1234567, 987654321, 54321),
  CostB = c(10000000, 2500, 99999999),
  Value = c(10, 20, 30)
)

# Get max cost
max_cost(df)

# Using a different column prefix
df_renamed <- rename(df, ExpenseA = CostA, ExpenseB = CostB)
max_cost(df_renamed, col_starts_with = "Expense")

}
